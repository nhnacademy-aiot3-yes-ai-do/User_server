spring:
  application:
    name: user-server

  cloud:
    eureka:
      client:
        enabled: false
    kubernetes:
      discovery:
        enabled: false

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:user_db;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS AUTH
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: AUTH

  jwt:
    secret: dGVzdC1qd3Qtc2VjcmV0LWtleS1tdXN0LWJlLWF0LWxlYXN0LTI1Ni1iaXRzLWxvbmctZm9yLWhzMjU2LWFsZ29yaXRobS10ZXN0
    access-token-expiration-ms: 1800000
    refresh-token-expiration-ms: 1209600000

  data:
    redis:
      host: 220.67.216.14
      port: 6379
      database: 310
      password: '*N2vya7H@muDTwdNMR!'

  mail:
    host: smtp.naver.com
    port: 465
    username: mush_official@naver.com
    password: ${MAIL_PASSWORD:mush970102!}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            trust: smtp.naver.com
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false

logging:
  level:
    root: INFO

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/